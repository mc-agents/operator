{{ include "mc-agents-operator.fullname" . }} is watching {{ with include "mc-agents-operator.watchNamespaces" . }}{{ . }}{{ else }}every namespace{{ end }}.

The operator stays in {{ .Release.Namespace }}. Everything it runs goes where it is asked for: an
MCP server in the namespace of its MCPServer, a bot in the namespace of its MinecraftBot.

Start an MCP server in a namespace of your own:

  kubectl create namespace game
  kubectl apply -n game -f - <<'EOF'
  apiVersion: mc-agents.junhyung.cloud/v1alpha1
  kind: MCPServer
  metadata:
    name: mc-agents
  spec: {}
  EOF

  kubectl get mcpservers -n game -w

Its bearer token is in the Secret named by status.tokenSecretRef, and join-server creates bots in
the same namespace. Set image tags for that namespace's bots with a MinecraftBotProfile named
"default", or for the whole cluster with a ClusterMinecraftBotProfile named "default".

Watch bots come up:

  kubectl get minecraftbots -n game -w

The operator is not on the data path. A bot pod dials the MCP server named in its own spec; LINK in
`kubectl get minecraftbots` is that pod's readiness probe, which a bot turns green only once its
hello has been accepted.

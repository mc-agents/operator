# Precedence fixture for verify-k3d.sh: the tags are not images, they are labels the assertions
# read back from status.image to tell which profile a bot was built from.
apiVersion: mc-agents.junhyung.cloud/v1alpha1
kind: ClusterMinecraftBotProfile
metadata:
  name: default
spec:
  fabric:
    tag: cluster-default
  azalea:
    tag: cluster-default
---
apiVersion: mc-agents.junhyung.cloud/v1alpha1
kind: MinecraftBotProfile
metadata:
  name: default
spec:
  azalea:
    tag: namespace-default
